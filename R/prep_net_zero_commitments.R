prep_net_zero_commitments <- function(results_portfolio,
                                      peers_results_aggregated,
                                      net_zero_targets) {
  browser()
  results_portfolio
  peers_results_aggregated
  net_zero_targets

  # TODO: this requires isins on the peer group level. open to discuss how to best get them here
  return(data_out)
}
